import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'dev.acima.ionic',
  appName: 'Acima',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
