{
  "name": "@acima/server",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "tsc --noEmit && node build.js",
    "dev": "tsx watch --clear-screen=false src/index.ts",
    "lint": "eslint src --quiet",
    "preview": "node --enable-source-maps ../../dist/apps/server/index.js"
  },
  "dependencies": {
    "@acima/util": "*",
    "express": "^4.18.1"
  },
  "devDependencies": {
    "@acima/eslint-config-custom": "*",
    "@acima/tsconfig": "*",
    "@types/express": "^4.17.13",
    "@types/node": "^18.7.15",
    "esbuild": "^0.15.7",
    "tsx": "^3.9.0",
    "typescript": "^4.8.2"
  }
}
