import { TodoListLoading } from './TodoListLoading';

export default {
  title: 'Todos'
};

export const TodoStory = () => {
  return <TodoListLoading />;
};

TodoStory.storyName = 'TodoListLoading';
